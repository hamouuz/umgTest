{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
           
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}" class="btn btn-primary">Retour</a>

    <a href="{{ path('app_admin_edit', {'id': user.id}) }}" class="btn btn-secondary">Modifier</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
